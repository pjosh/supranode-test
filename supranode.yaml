script:
  ## Install dependencies (alternatives commented)
  # - yarn install
  # - pnpm install
  # - bun install
  - npm install
  ## Build (alternatives commented)
  # - react-scripts build
  - npm run build
artifact: build
